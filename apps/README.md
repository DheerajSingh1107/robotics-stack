# Custom NuttX Apps

Place product-specific firmware apps here (for example `apps/your_app/`).

This directory is intentionally separate from the `nuttx-apps` submodule so custom code can evolve independently while NuttX dependencies stay pinned.
